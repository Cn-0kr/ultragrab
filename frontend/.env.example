# 生产站点根 URL（无尾部斜杠）。构建时用于 canonical、Open Graph、Twitter Card、
# robots.txt / sitemap.xml 中的绝对地址。
#
# 本地或未设置时：不注入依赖绝对 URL 的 meta；robots.txt 仅含 Allow，不写入 Sitemap 行，且不生成 sitemap.xml。
#
# 示例（复制一行到 .env.production 或构建环境）：
# VITE_SITE_ORIGIN=https://www.example.com

# 可选。og:image / twitter:image 的完整 URL；未设置且已配置 VITE_SITE_ORIGIN 时，回退为 {VITE_SITE_ORIGIN}/favicon.svg。
# 建议后续在 public/ 放置 1200×630 专用图并在此处填写完整 URL。
# VITE_OG_IMAGE=https://www.example.com/og/ultrgrab-1200x630.png
